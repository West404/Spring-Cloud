server:
  port: ${port:9091}
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
  application:
    name: user-service
mybatis:
  type-aliases-package: com.isoftstone.user.domain
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka